query Locations($page: Int) {
  locations(page: $page) {
    results {
      id
      name
      type
      dimension
      residents {
        name
        image
        id
      }
    }
    info {
      count
      next
      pages
      prev
    }
  }
}
